import React from 'react';
import { Title } from "../components"

const Home = (props) => {

    return (
        <div className="home text-center h-100">
            <Title />
        </div>
    );
};

export default Home;